{
  "name": "@ninery/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @ninery/player-intelligence build && pnpm --filter @ninery/equipment-intelligence build && pnpm --filter @ninery/recommendation-intelligence build && tsx watch src/main.ts",
    "build": "pnpm --filter @ninery/player-intelligence build && pnpm --filter @ninery/equipment-intelligence build && pnpm --filter @ninery/recommendation-intelligence build && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "start:prod": "node scripts/start-prod.mjs",
    "lint": "pnpm --filter @ninery/player-intelligence build && pnpm --filter @ninery/equipment-intelligence build && pnpm --filter @ninery/recommendation-intelligence build && tsc -p tsconfig.json --noEmit",
    "typecheck": "pnpm --filter @ninery/player-intelligence build && pnpm --filter @ninery/equipment-intelligence build && pnpm --filter @ninery/recommendation-intelligence build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test \"dist/**/*.test.js\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@ninery/equipment-intelligence": "workspace:*",
    "@ninery/player-intelligence": "workspace:*",
    "@ninery/recommendation-intelligence": "workspace:*",
    "@prisma/client": "^6.1.0",
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.0.1",
    "@nestjs/platform-express": "^11.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
